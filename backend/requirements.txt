# ─────────────────────────────────────────────
# PSX Portfolio Tracker — Python Dependencies
# ─────────────────────────────────────────────

# Web Framework
Flask==3.0.3
Flask-RESTful==0.3.10

# Database ORM & Migrations
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9          # PostgreSQL driver (Supabase uses Postgres)

# PSX Market Data
psxdata==0.1.0a5                  # Pakistan Stock Exchange EOD data library

# Data Manipulation
pandas==2.2.2
numpy==1.26.4

# Environment & Config
python-dotenv==1.0.1

# Date / Time Utilities
python-dateutil==2.9.0

# HTTP Requests (used internally by psxdata)
requests==2.32.3

# Logging & Utilities
colorlog==6.8.2

# Authentication — Supabase JWT verification (HS256)
PyJWT==2.13.0
cryptography==49.0.0   # Required by PyJWT for RS256 support (and key ops)


# Testing
pytest==8.2.2
pytest-flask==1.3.0
pytest-cov==5.0.0
factory-boy==3.3.0              # Model factories for tests
